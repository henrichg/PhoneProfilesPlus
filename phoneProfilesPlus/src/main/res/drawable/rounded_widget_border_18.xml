<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" android:padding="0dp">

    <solid android:color="@color/pppColorTransparent"/>

    <corners
        android:bottomRightRadius="18dp"
        android:bottomLeftRadius="18dp"
        android:topLeftRadius="18dp"
        android:topRightRadius="18dp"/>

    <!-- must be white, used is color filter for change color -->
    <stroke android:width="1dp" android:color="@color/pppColorWhiteWithAlpha"/>

</shape>
